# Build / IDE / tooling
.gradle/
.kotlin/
build/
*/build/
local.properties
.idea/
*.iml
captures/

# Signing keys (never commit)
keystore/
*.jks
*.keystore

# Bundled JDK (large, not source)
jdk-17/

# Timestamped backups & snapshots (git history already protects these)
snap-*.zip
*.bak-*

# Working/scratch files
*.log
